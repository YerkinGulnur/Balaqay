# 🌱 Balaqay — Инструкция по установке (Windows)

> Полное руководство для начинающих. Выполняйте шаги по порядку.

---

## Шаг 1 — Установите необходимые программы

Перед началом нужно установить 4 программы. Скачайте и установите каждую:

### 1.1 Git
> Нужен для скачивания кода проекта с GitHub

1. Перейдите на сайт: **https://git-scm.com/download/win**
2. Нажмите **"Click here to download"**
3. Откройте скачанный `.exe` файл и нажимайте **Next** на всех экранах → **Install**
4. После установки закройте окно

### 1.2 Node.js + npm
> Нужен для запуска фронтенда и бэкенда

1. Перейдите на сайт: **https://nodejs.org**
2. Скачайте версию с надписью **"LTS"** (левая кнопка)
3. Откройте скачанный `.msi` файл и нажимайте **Next** → **Install**
4. После установки закройте окно

### 1.3 PostgreSQL
> База данных, в которой хранятся все данные приложения

1. Перейдите на сайт: **https://www.postgresql.org/download/windows/**
2. Нажмите **"Download the installer"**
3. Выберите последнюю версию для Windows x86-64 и скачайте
4. Откройте скачанный файл и нажимайте **Next**
5. На экране **"Password"** придумайте и запомните пароль — он понадобится позже!
6. Порт оставьте **5432** (по умолчанию)
7. Дожидайтесь конца установки → **Finish**

> ⚠️ **Запомните пароль от PostgreSQL** — он потребуется на Шаге 4!

### 1.4 IDE (редактор кода)
> Программа для просмотра и редактирования кода

1. Перейдите на сайт: **https://code.visualstudio.com**
2. Нажмите **"Download for Windows"**
3. Установите, нажимая **Next** → **Install**

---

## Шаг 2 — Проверьте установку

1. Нажмите `Win + R`, введите `cmd`, нажмите **Enter** — откроется чёрное окно (командная строка)
2. Введите каждую команду и нажимайте **Enter**:

```
git --version
```
Должно показать что-то вроде: `git version 2.44.0`

```
node --version
```
Должно показать: `v20.11.0` (или другую версию)

```
npm --version
```
Должно показать: `10.2.4` (или другую версию)

> Если какая-то команда не найдена — **перезапустите командную строку** и попробуйте снова.

---

## Шаг 3 — Скачайте проект

1. В командной строке перейдите в папку, куда хотите сохранить проект.  
   Например, на рабочий стол:

```
cd %USERPROFILE%\Desktop
```

2. Скачайте проект с GitHub:

```
git clone git@github.com:NurzhanTng/balaqay.git
```

3. Зайдите в папку проекта:

```
cd balaqay
```

---

## Шаг 4 — Настройте и запустите бэкенд

### 4.1 Установите зависимости

```
cd backend
```

```
npm install
```

> Ждите пока установятся все пакеты (1–3 минуты)

### 4.2 Создайте файл настроек

```
copy .env.example .env
```

**Пример заполненного файла:**
```
NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=kidgrow

JWT_SECRET=kidgrow_super_secret_key_2024
JWT_EXPIRES_IN=7d
```

Сохраните файл: `Ctrl + S`

### 4.3 Создайте базу данных и таблицы

Выполните три команды по очереди:

```
npm run db:create
```

```
npm run db:migrate
```

```
npm run db:seed
```

> После `db:seed` вы увидите сообщение:
> ```
> ✅ Seed complete!
> Demo login: demo@balaqay.app / password
> ```

### 4.4 Запустите бэкенд

```
npm run start:dev
```

> Вы увидите:
> ```
> 🚀 Balaqay API running on http://localhost:3000/api/v1
> 📚 Swagger docs at  http://localhost:3000/api/docs
> ```

✅ **Бэкенд работает!** Не закрывайте это окно.

---

## Шаг 5 — Запустите фронтенд

Откройте **новое** окно командной строки (`Win + R` → `cmd`) и выполните:

### 5.1 Перейдите в папку фронтенда

```
cd %USERPROFILE%\Desktop\balaqay\frontend
```

### 5.2 Установите зависимости

```
npm i
```

> Ждите пока установятся все пакеты (1–2 минуты)

### 5.3 Запустите фронтенд

```
npm run dev
```

> Вы увидите:
> ```
>   VITE v5.1.0  ready in 300 ms
>   ➜  Local:   http://localhost:5173/
> ```

✅ **Фронтенд работает!**

---

## Шаг 6 — Откройте приложение

Откройте браузер и перейдите по адресу:

## 👉 http://localhost:5173

Вы можете войти с демо-аккаунтом:
- **Email:** `demo@balaqay.app`
- **Пароль:** `password`

---

## 🔄 Как запускать в следующий раз

Каждый раз при запуске нужно открыть **два** окна командной строки:

**Окно 1 — Бэкенд:**
```
cd %USERPROFILE%\Desktop\balaqay\backend
npm run start:dev
```

**Окно 2 — Фронтенд:**
```
cd %USERPROFILE%\Desktop\balaqay\frontend
npm run dev
```

Затем откройте **http://localhost:5173** в браузере.

---

## ❓ Частые проблемы

| Проблема | Решение |
|----------|---------|
| `git` не найден | Перезапустите командную строку после установки Git |
| `node` не найден | Перезапустите командную строку после установки Node.js |
| Ошибка подключения к БД | Проверьте что PostgreSQL запущен и пароль в `.env` правильный |
| Порт занят | Перезагрузите компьютер и попробуйте снова |
| `npm run db:create` не работает | Убедитесь что PostgreSQL запущен (Win → поиск → "Services" → PostgreSQL → Start) |

---

> 💡 **Подсказка:** Если что-то не работает — скопируйте текст ошибки и обратитесь к разработчику.
